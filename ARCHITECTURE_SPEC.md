# K-Tax MCP Architecture Specification (v3.0)

이 문서는 2026-09-14 사용자 설명에 맞춰 정리한 목표 명세다. 아래 기능이 현재 코드에 모두 구현됐다는 뜻은 아니다.

2026-09-14 구현 전략 변경: 법제처 파서 원본을 이식하는 대신 Express가 MCP Client가 되어 공식 `korean-law-mcp`를 별도 stdio 프로세스로 호출한다. upstream 코드는 패키지 업데이트로 유지하며, 서버 배포 시 업데이트 검사·검증·전환·실패 복구를 수행하는 cron을 함께 구성한다. 기존 로컬 cron 파일을 전제로 하지 않는다. 이번 구현 범위와 실행·갱신 방법은 [README.md](README.md)에 기록한다. 이 내부 MCP 연결만으로 외부 호출자용 표준 MCP 서버나 자동 패치·AI 검수 전체가 완성된 것은 아니다.

## 1. 목적과 필수 조건

이 프로젝트는 특정 LLM에 종속되지 않는 법령·예규 조회 및 검증 MCP 서버다. 사용자는 원하는 모델을 MCP 지원 클라이언트에서 사용한다. MCP 자체에서 발견한 실패는 패치로 개선하며, 1차 AI 검수와 자동 검증을 거친 뒤 2차로 사람이 최종 반영을 승인한다.

- 표준 MCP로 연결한다. 특정 모델 공급자, 특정 클라이언트의 전용 기능, 전용 Web/Slack UI를 필수 조건으로 두지 않는다.
- 일반 질문의 추론과 답변 작성은 사용자가 선택한 LLM이 담당한다.
- MCP 서버는 법령 API 조회, 필요한 크롤링, 근거 제공, 파이프라인 안내 및 실행 가능한 검증 도구를 제공한다.
- 실패 수집, 패치 작성·재수정, AI 검수, PR, 사람의 최종 승인은 초기 핵심 기능에 포함한다.
- 패치 대상은 실패 규칙뿐 아니라 필요한 검색·수집·검증 코드와 재현 테스트도 포함한다.
- 무료 티어에서 장기간 운영하는 것을 설계 조건으로 둔다. 자동 개선을 제외하거나 유료 전환을 전제해 이 조건을 대신하지 않는다.
- 자료의 적용 시점과 최신성 확인은 비용 절감 과정에서도 유지한다.

## 2. 일반 사용과 유지보수의 역할 분리

| 역할 | 책임 |
| --- | --- |
| 사용자의 LLM·MCP 클라이언트 | 도구 호출, 사용자 사실관계 정리, 반환된 근거와 검증 결과를 이용한 답변 작성 |
| MCP 서버 | 표준 도구 제공, 원천 조회와 수집, 버전 확인, 검증 실행, 실패·개선 상태 보관 |
| 패치 작성 작업 | 실패 재현, 변경안과 테스트 작성, 검수 지적에 따른 재수정 |
| 1차 AI 검수 작업 | 제안 패치와 근거·테스트 결과를 별도 검수 단계에서 확인하고 통과·수정 필요·검증 불가를 반환 |
| 사람 | 검증을 마친 PR의 최종 검토와 반영 승인 |

검수용 AI는 MCP를 사용하는 모델과 별도로 구성하고 교체 가능하게 한다. 일반 MCP 호출에 검수용 모델을 사용하도록 강제하지 않는다. 클라이언트가 자체 AI 호출 기능을 지원하지 않아도 기본 MCP 도구를 사용할 수 있어야 한다. 검수 AI의 공급자와 실행 위치는 구현 시 연결 설정으로 결정한다.

Express는 원격 HTTP 요청을 받는 구현 선택지다. 별도 서버 제품이나 LLM 실행 엔진이 아니다. 일반 HTTP API만 구현한 것으로 MCP 지원이 완료되지는 않는다. MCP 프로토콜 처리를 복원하고, HTTP·로컬 연결에 필요한 어댑터와 도구의 핵심 로직을 분리한다. 지원하는 연결 방식은 실제 클라이언트별로 검증한다.

## 3. 법령·예규 조회와 검증

기존 [품질 원칙](conversation-derived-legal-quality-gates.md)의 A~E를 유지한다. [pipelines.yaml](pipelines.yaml)의 WP-01 현행법, WP-02 과거 사건, WP-03 예외·해석 충돌 경로를 MCP 도구와 연결한다. [fail-cases.yaml](fail-cases.yaml)의 규칙은 적용 조건을 판정하고, 실행 가능한 검증과 근거 대조에 사용한다.

### 자료를 사용할 때의 확인 절차

1. 사건 기준일을 정한다. 현재 시행 중인 법령과 과거 사건에 적용할 법령을 구분하고, 필요한 부칙·적용례·경과조치를 확인한다.
2. 공식 원천의 법령 식별자, 버전, 시행일과 변경 정보를 확인한다. 동일한 버전의 저장 본문은 이 확인 후 재사용한다. 변경 정보만으로 확인할 수 없으면 원문을 다시 조회한다.
3. 시행령·시행규칙·별표·준용조문도 근거에 연결한다. 상위 법령 본문이 같아도 연결된 근거가 바뀌면 영향을 다시 확인한다.
4. 예규는 문서 원문 확인에 더해 관련 법령 개정, 후속 해석·해석 정비 및 관련 판결을 확인한다. 오래된 문서가 원천에 존재한다는 사실만으로 현행 적용이 확인됐다고 판단하지 않는다.
5. MCP 결과에 출처, 문서·버전 식별자, 적용 기준일, 원천 확인 시각, 확인 범위와 미확인 사항을 포함한다. 원문 확보와 적용 여부 검증의 상태를 구분한다.

최신성 또는 적용 여부를 확인하지 못하면 검증 불가 상태를 반환한다. 저장된 자료를 참고용으로 제공하더라도 현행 근거 확인 완료나 검증 통과로 표시하지 않는다.

변경이 감지된 근거를 사용하는 캐시 결과와 실패 규칙에는 재검증 필요 상태를 부여한다. 규칙 수정이 필요하면 아래 패치·검수·승인 경로를 따른다. 과거 사건용 버전은 이력을 보존한다.

법제처의 목록·연혁·변경 이력 및 본문 API는 [공식 API 가이드](https://open.law.go.kr/LSO/openApi/guideList.do)를 기준으로 구현한다. 예규와 해석 정비의 원천은 [국세법령정보시스템](https://taxlaw.nts.go.kr/)에서 확인한다. 실제 응답과 수집 성공 여부는 구현 테스트에서 별도로 확인해야 한다.

## 4. MCP 자체의 반자동 개선

실패 기록 → 재현 사례 → 패치 후보와 PR → 자동 검증 및 1차 AI 검수 → 필요 시 재수정 → 2차 사람 승인 → 승인된 버전 반영

1. 실패의 입력·기준일·원천 근거·기대 결과·실제 결과·MCP 버전을 기록한다. 사용자와 LLM의 지적은 검증할 실패 후보로 접수한다.
2. 패치 작업이 실패를 재현하고 수정 범위를 정한다. 검색 누락, 수집 오류, 버전 선택 오류, 규칙 판정 오류 등 원인에 맞는 코드·규칙·테스트를 변경한다.
3. Octokit으로 PR 브랜치를 만들거나 기존 제안의 PR을 업데이트한다. 같은 실패에 대한 중복 제안을 식별한다.
4. 해당 패치의 빌드, 스키마 검사, 실패 재현 및 기존 회귀 테스트를 실행한다. 법률적 주장은 적용 시점에 맞는 원천 근거를 첨부한다. 코드 테스트 통과만으로 법률적 타당성을 확정하지 않는다.
5. 별도 AI 검수 단계가 패치 차이, 실패 사례, 근거와 자동 검증 결과를 평가한다. 작성자의 성공 선언이나 특정 단어 포함 여부만으로 승인하지 않는다.
6. 검수 실패 사유를 패치 작업으로 돌려보내 수정하고 다시 검증한다. 실행 불가·외부 한도·반복 실패 상태는 저장하여 관리하며 미완료 작업을 통과 처리하지 않는다.
7. 자동 검증과 AI 검수 결과를 특정 패치 커밋에 연결한다. 패치가 바뀌면 그 변경에 필요한 검증을 다시 수행한다. 사람은 최종 검토할 패치와 그 패치의 검수 결과를 함께 확인한다.
8. 사람의 최종 승인 후 병합하고 승인된 규칙·코드 버전을 배포한다. 운영 중인 버전과 이전 버전을 식별하고 되돌릴 수 있게 한다.

자동 수정은 제안 브랜치에서 진행한다. AI 검수 통과는 사람의 최종 승인을 대체하지 않는다. 운영 규칙과 서비스 코드에는 승인된 버전이 반영된다.

## 5. 무료 티어에서 지속 운영

- 원천 버전 확인을 유지하면서 동일 본문 다운로드, 중복 검색과 중복 수집을 줄인다.
- 동일 자료에 대한 동시 요청은 하나의 수집 작업으로 합치고 그 결과를 공유한다.
- 일반 조회와 개선 작업의 동시 실행 수를 자원에 맞춰 조절한다. 부하가 몰린 작업은 상태를 보관하여 순서대로 처리한다.
- 수집·개선 작업에는 진행 상태, 재시도 시점과 중복 실행 방지 정보를 보관한다. 프로세스 재시작이나 외부 한도 때문에 이미 수행한 변경을 무조건 반복하지 않는다.
- 크롤링과 패치 검수는 필요한 경우에 실행한다. 검증된 데이터와 변경 이력을 재사용하고, 로그 보관량을 관리한다.
- 외부 무료 한도가 소진되면 관련 작업을 대기 상태로 보관해 가능한 시점에 재개한다. 필요한 최신성 확인이 막힌 요청에는 이를 명시하고 확인 완료 결과를 만들지 않는다.
- 서버 무료 제공량과 외부 수집·검수 서비스의 제공량을 함께 측정한다. 외부 서비스의 처리량까지 무제한이라고 가정하지 않는다.

서버 사양, 동시 처리 수, 갱신 주기와 외부 수집 경로는 실제 자료 크기·수집 성공률·호출량으로 결정한다. 특정 무료 인스턴스에서 PM2 재시작 설정만으로 OOM 방지나 지속 연결이 보장된다고 간주하지 않는다.

## 6. 기존 기술 선택의 위치

- Node.js와 TypeScript: MCP 도구와 공통 로직의 기존 구현 기반.
- Express: 필요 시 MCP HTTP 연결과 보조 상태 API의 진입점.
- Supabase: 캐시 메타데이터, 실패 사례, 개선 작업 상태, 사용자 접근 제어를 위한 저장소 후보. 요청마다 신원을 검증하고 필요한 데이터 접근 범위를 구분한다.
- Octokit: GitHub 브랜치, 파일 변경, PR과 검수 상태 연동.
- Supreme Judge: 패치의 1차 AI 검수 역할. 특정 모델에 고정하거나 일반 사용자 질의를 대신하는 엔진으로 확장하지 않는다.
- 외부 Scraping API: 공식 API·일반 HTTP 조회로 확보할 수 없는 자료의 수집 후보. TXSI 성공 여부와 무료 제공량 안에서의 지속 가능성을 검증하고 선택한다.
- GCE와 PM2: 원격 배포 후보. 배포 환경 선택과 MCP의 기능·검증 규칙을 분리한다.

## 7. 현재 코드와 목표의 차이

- src/index.ts는 일반 Express API 골격이며 MCP 프로토콜 처리가 없다. 법령 조회·캐시는 Mock 응답이다.
- src/supremeJudge.ts는 키워드 검사와 고정 지연을 사용하는 데모이며 AI 검수가 구현되지 않았다.
- src/gitOps.ts는 고정 PR 주소를 반환한다. 실제 PR 생성·업데이트·검수 상태 연동은 구현되지 않았다.
- validator.py는 일부 예시 휴리스틱만 구현했고 규칙의 발동 조건을 평가하지 않는다. 아직 모든 품질 게이트가 실행되는 검증기가 아니다.
- pipelines.yaml과 fail-cases.yaml은 의도와 규칙의 출발점이며 현재 서버 실행 경로와 연결되지 않았다.

## 8. 구현 시 준수할 사항

- 서비스의 GitHub 변경은 @octokit/rest로 수행한다. 서비스 코드에서 로컬 git 또는 exec 호출로 PR을 생성하지 않는다.
- 경량 서버에서 puppeteer 또는 playwright를 실행하지 않는다. 외부 수집은 API용 HTTP 클라이언트로 구현한다.
- 제안된 코드의 실행 검사는 서비스 프로세스와 분리된 검증 환경에서 수행한다.
- 비동기 실패와 외부 API·DB의 반환 오류를 명시적으로 처리한다. 실패한 저장이나 미실행 작업을 성공으로 반환하지 않는다.
- Supabase의 기본 클라이언트 등 재사용 가능한 연결 설정을 요청 루프 밖에 두되, 요청별 사용자 인증 상태를 공유 가변 상태로 섞지 않는다.
- 생성 규칙은 공통 스키마로 검증하고 YAML 직렬화기를 사용한다. 필요한 발동 조건과 근거 정보가 없는 규칙을 승인된 규칙으로 반영하지 않는다.
- 고정된 Mock 성공 응답을 실서비스 결과로 제공하지 않는다.
